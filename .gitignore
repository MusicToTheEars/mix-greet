# Local Netlify folder
.netlify
# No trailing slash: this is sometimes a symlink to a store outside Dropbox,
# and `node_modules/` does not match a symlink.
node_modules

# Internal planning/spec docs — NOT for the public deploy (Netlify publishes repo root)
Grilled.md
PLAN*.md
openspec/
.env
.env.*
convex/_generated/
.claude/

# macOS
.DS_Store

# Local wallet test rig — throwaway certs and generated passes. Never committed.
.wallet-test/
